/* Every method is fault-tolerant on purpose: a browser/hardware audio quirk must never be able to block game logic (answering, finishing, etc). `broken` latches on the first failure so a device where audio never works pays that cost once, not on every single note — repeatedly retrying a failing AudioContext construction on every answer measurably slows the whole page down. */
const Audio={ctx:null,broken:false,on(){if(this.broken||this.ctx)return;try{this.ctx=new (window.AudioContext||window.webkitAudioContext)()}catch(e){this.broken=true;console.error('Audio disabled (AudioContext unavailable):',e)}},tone(f,d,type='sine',drop=false){if(this.broken)return;try{this.on();if(!this.ctx)return;let o=this.ctx.createOscillator(),g=this.ctx.createGain();o.type=type;o.frequency.setValueAtTime(f,this.ctx.currentTime);if(drop)o.frequency.exponentialRampToValueAtTime(80,this.ctx.currentTime+d);g.gain.setValueAtTime(.09,this.ctx.currentTime);g.gain.exponentialRampToValueAtTime(.001,this.ctx.currentTime+d);o.connect(g).connect(this.ctx.destination);o.start();o.stop(this.ctx.currentTime+d)}catch(e){this.broken=true;console.error('Audio disabled after playback failure:',e)}},correct(){[430,620,800].forEach((f,i)=>setTimeout(()=>this.tone(f,.22,'triangle'),i*70))},wrong(){this.tone(280,.35,'sawtooth',true)}};
